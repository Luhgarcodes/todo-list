import React from 'react'

const Title = () => {
    // const date = new Date();
    // const d = `${date.getDate()}:${date.getMonth() + 1}:${date.getFullYear()}`;

    return (
        <div className="title">
            <h1>Todo list
            </h1>

        </div>
    )
}

export default Title